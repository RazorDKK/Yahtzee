class terning {
  constructor(number, x, y) {
    this.number = number;
    this.x = x;
    this.y = y;
  }
  Pointskema;
  draw() {}

  roll() {
    if ((this.number = 0)) {
      this.number = floor(random(1, 6));
    }
  }
}
